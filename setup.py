from setuptools import setup

setup(
    name='CollectionMenager',
    version='0.1',
    description='Collection Menager App',
    author='Robin',
    packages=[],
    include_package_data=True,
    install_requires=[],
)
